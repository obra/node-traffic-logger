#!/usr/bin/env node

// ABOUTME: CLI script for viewing HAR files generated by node-traffic-logger
// ABOUTME: Provides command-line interface for displaying and formatting HTTP Archive data

const path = require('path');
const fs = require('fs');
const dumpHarPath = path.join(__dirname, '..', 'scripts', 'dump-har.cjs');

// Just delegate to the dump-har.cjs script
require(dumpHarPath);